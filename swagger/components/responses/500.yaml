description: Internal server error.
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 500
        message:
          type: string
          example: 'Something went wrong'
        data:
          type: string
          example: 'Error info'
    example:
      status: 500
      message: 'Something went wrong'
      data: 'Error info'
